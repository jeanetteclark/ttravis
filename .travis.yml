# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
cache: packages
addons:
  apt:
    packages:
      - libraptor2-dev #dependancy
      - librasqal3-dev #dependancy
      - librdf0-dev #dataone
      - libcurl4-openssl-dev

